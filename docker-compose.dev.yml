# Lokale Entwicklungsumgebung — baut aus Source Code
#
# Starten:        docker compose -f docker-compose.dev.yml up --build
# Nur Backend:    docker compose -f docker-compose.dev.yml up postgres backend
# Frontend-Dev:   cd frontend && npm run dev  (mit Hot Reload, empfohlen)

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: up2daite
      POSTGRES_USER: up2daite
      POSTGRES_PASSWORD: up2daite
    ports:
      - "45432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U up2daite"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "48080:8080"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: up2daite
      DB_USER: up2daite
      DB_PASSWORD: up2daite
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "43000:8080"
    depends_on:
      - backend
    restart: unless-stopped

  # prometheus:
  #   image: prom/prometheus:latest
  #   ports:
  #     - "49090:9090"
  #   volumes:
  #     - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #     - prometheus_data:/prometheus
  #   command:
  #     - '--config.file=/etc/prometheus/prometheus.yml'
  #     - '--storage.tsdb.path=/prometheus'
  #   depends_on:
  #     - backend
  #   restart: unless-stopped

  # grafana:
  #   image: grafana/grafana:latest
  #   ports:
  #     - "43001:3000"
  #   environment:
  #     GF_SECURITY_ADMIN_PASSWORD: admin
  #     GF_USERS_ALLOW_SIGN_UP: "false"
  #   volumes:
  #     - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
  #     - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
  #     - grafana_data:/var/lib/grafana
  #   depends_on:
  #     - prometheus
  #   restart: unless-stopped

volumes:
  postgres_data:
  prometheus_data:
  grafana_data:
