name: todo-app

# Produktionsnahe Umgebung — zieht Images aus GitLab Registry
#
# Starten:        docker compose up
# Images updaten: docker compose pull && docker compose up -d
#
# Für lokale Entwicklung mit Build: docker compose -f docker-compose.dev.yml up --build
#
# Ports (Präfix 5 zur Isolation von anderen lokalen Projekten):
#   Frontend:   http://localhost:53000
#   Backend:    http://localhost:58080
#   Postgres:   localhost:55432
#   Prometheus: http://localhost:59090
#   Grafana:    http://localhost:53001

services:

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: todoapp
      POSTGRES_USER: todoapp
      POSTGRES_PASSWORD: todoapp
    ports:
      - "55432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U todoapp"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: registry.gitlab.com/andre00111/todoapp/backend:latest
    ports:
      - "58080:8080"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: todoapp
      DB_USER: todoapp
      DB_PASSWORD: todoapp
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    image: registry.gitlab.com/andre00111/todoapp/frontend:latest
    ports:
      - "53000:8080"
    depends_on:
      - backend
    restart: unless-stopped

  # prometheus:
  #   image: prom/prometheus:latest
  #   ports:
  #     - "59090:9090"
  #   volumes:
  #     - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #     - prometheus_data:/prometheus
  #   command:
  #     - '--config.file=/etc/prometheus/prometheus.yml'
  #     - '--storage.tsdb.path=/prometheus'
  #   depends_on:
  #     - backend
  #   restart: unless-stopped

  # grafana:
  #   image: grafana/grafana:latest
  #   ports:
  #     - "53001:3000"
  #   environment:
  #     GF_SECURITY_ADMIN_PASSWORD: admin
  #     GF_USERS_ALLOW_SIGN_UP: "false"
  #   volumes:
  #     - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
  #     - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
  #     - grafana_data:/var/lib/grafana
  #   depends_on:
  #     - prometheus
  #   restart: unless-stopped

volumes:
  postgres_data:
  # prometheus_data:
  # grafana_data:
